name: Event Filters

on:
  pull_request:
    types:
      - opened
      - synchronize
      - closed

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Hello world
        run: echo "Hello world"
